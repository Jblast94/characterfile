## 2024-05-22 - [CLI Prompt UX]
**Learning:** Using `inquirer`'s specific types (list, confirm, password) instead of generic text inputs significantly reduces user error and improves security (masking keys) in CLI tools.
**Action:** Always prefer specific prompt types over generic inputs for structured choices or secrets.
