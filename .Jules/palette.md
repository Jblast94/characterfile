## 2024-05-22 - CLI Prompt UX
**Learning:** CLI tools often rely on raw text input for fixed options, causing typos and cognitive load.
**Action:** Always check if a text input in a CLI can be replaced by a `list`, `checkbox`, or `confirm` prompt using libraries like `inquirer`.
